{
  "name": "waitinglist-bazodiac",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bazodiac FuFirE Fusion Preview: standalone frontend plus Railway-ready contract API adapter.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
